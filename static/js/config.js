// config.js

// Definición global de twpConfig
window.twpConfig = {
    apiBaseUrl: "http://localhost:5000",  // Solo la URL base
    // Ejemplo de configuración
    theme: "dark",
    debug: true,
    // Otras configuraciones que necesites...
};

console.log("twpConfig cargado correctamente", window.twpConfig);  // Esto te ayudará a verificar si twpConfig está definido correctamente
